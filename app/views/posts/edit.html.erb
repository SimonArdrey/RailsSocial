<section class="mt-2">
  <h1>Editing Post</h1>

  <%= render 'form', post: @post, postable: @postable %>

  <%= link_to 'Show', polymorphic_path([@postable, @post]) %> |
  <%= link_to 'Back', polymorphic_path([@postable, Post]) %>
</section>
