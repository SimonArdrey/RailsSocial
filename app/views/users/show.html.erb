<section class="mt-2">
  <% if @postable %>
    <%= render 'posts/form', post: @post, postable: @postable %>
  <% end %>

  <% @posts.each do |post| %>
   <%= render 'posts/shared/post', post: post, postable: @postable %>
  <% end %>
</section>
